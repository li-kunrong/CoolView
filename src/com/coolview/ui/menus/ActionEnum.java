package com.coolview.ui.menus;

public enum ActionEnum {
    openI, close, path, 
    cut, copy, paste, delete,rename,paint,
    big,middle,small,
    name,date,type,size,
    increase,decrease,
    update,about,
    repaint,theme,windows,info,author,
    default0,mac,selectAll
}
